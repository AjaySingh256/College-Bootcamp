#include<stdio.h>
int main(){
    int n;
    printf("Enter the number:");
    scanf("%d",&n);
    if(n>0)
        printf("number is postive\n");
        else if(n<0)
        printf("number is negative\n");
        else
        printf("number is zero");
        return 0;
}
